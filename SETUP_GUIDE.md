# SMS 인증번호 자동 전달 — 설정 가이드

iPhone 단축어에서 받은 SMS를 웹훅 서버로 전송하고,
서버가 Telegram 또는 WeCom으로 인증번호를 전달합니다.

---

## 1단계: 서버 배포

### Option A: Render.com (무료, 추천)

1. 이 저장소를 GitHub에 push합니다.
2. [render.com](https://render.com) 에 가입 후 **New → Blueprint** 선택.
3. 저장소를 연결하면 `render.yaml`을 자동으로 읽습니다.
4. 서비스 생성 후 **Environment** 탭에서 아래 환경변수를 채웁니다:

   | 변수 | 값 |
   |---|---|
   | `AUTH_TOKEN` | 길고 랜덤한 문자열 (예: `openssl rand -hex 32` 결과) |
   | `TELEGRAM_BOT_TOKEN` | BotFather에서 받은 토큰 |
   | `TELEGRAM_CHAT_ID` | 본인 Chat ID |
   | `TELEGRAM_ENABLED` | `true` |
   | `WECOM_ENABLED` | `true` (필요 시) |
   | `WECOM_WEBHOOK_URL` | WeCom 봇 웹훅 URL |
   | `ALLOWED_SENDERS` | 허용할 발신번호 (쉼표 구분, 비워두면 전체 허용) |

5. 배포 완료 후 제공되는 URL을 기록합니다. (예: `https://sms-forwarder.onrender.com`)

> 주의: Render 무료 플랜은 15분 비활동 시 슬립 상태가 됩니다.
> 첫 요청 시 최대 30초 지연이 발생할 수 있지만 인증번호 유효시간(보통 3~5분) 내에는 충분합니다.

---

### Option B: Railway

1. [railway.app](https://railway.app) 에 가입 후 **New Project → Deploy from GitHub repo** 선택.
2. 저장소를 선택하고 `sms-forwarder/server` 디렉토리를 루트로 설정합니다.
3. **Variables** 탭에서 Option A와 동일한 환경변수를 입력합니다.
4. 자동 배포가 완료되면 Railway가 제공하는 도메인을 사용합니다.

---

### Option C: 로컬 실행 (ngrok 사용)

```bash
# 서버 실행
cd sms-forwarder/server
cp .env.example .env
# .env 파일에 값 채우기
pip install -r requirements.txt
uvicorn app:app --host 0.0.0.0 --port 8000

# ngrok으로 외부 노출
ngrok http 8000
# → https://xxxx.ngrok-free.app 형태의 URL 사용
```

---

## 2단계: Telegram 봇 설정

### 봇 생성

1. Telegram에서 **@BotFather** 를 검색하여 채팅을 시작합니다.
2. `/newbot` 명령어를 보냅니다.
3. 봇 이름과 사용자명(username, `_bot`으로 끝나야 함)을 입력합니다.
4. BotFather가 **Bot Token** 을 전달합니다. (예: `7123456789:AAFxxx...`)
5. 이 토큰을 서버의 `TELEGRAM_BOT_TOKEN` 환경변수에 입력합니다.

### Chat ID 확인

1. 생성한 봇을 검색해서 **/start** 메시지를 보냅니다.
2. 아래 URL을 브라우저에서 열어서 `chat.id` 값을 확인합니다:
   ```
   https://api.telegram.org/bot{TELEGRAM_BOT_TOKEN}/getUpdates
   ```
   응답 예시:
   ```json
   {
     "result": [{
       "message": {
         "chat": { "id": 123456789, ... }
       }
     }]
   }
   ```
3. `id` 값을 서버의 `TELEGRAM_CHAT_ID` 환경변수에 입력합니다.

---

## 3단계: WeCom 웹훅 설정

1. WeCom(企业微信) 앱에서 인증번호를 받을 **그룹 채팅**을 엽니다.
2. 그룹 이름 → **그룹 관리** → **봇 추가** → **새 봇 만들기** 를 선택합니다.
3. 봇 이름을 입력하고 완료하면 **웹훅 URL** 이 표시됩니다.
4. URL을 복사하여 서버의 `WECOM_WEBHOOK_URL` 환경변수에 입력합니다.

---

## 4단계: iPhone 단축어 설정

### 준비사항
- iOS 15.4 이상 (자동화 "바로 실행" 기능 필요)
- 배포된 서버 URL
- 서버에 설정한 `AUTH_TOKEN` 값

### 단계별 설정

**1. 단축어 앱 열기**
- 홈 화면에서 **단축어** 앱을 엽니다.

**2. 새 자동화 만들기**
- 하단의 **자동화** 탭을 탭합니다.
- 오른쪽 상단 **+** 를 탭합니다.
- **개인용 자동화 생성** 을 선택합니다.

**3. 트리거 설정**
- 목록에서 **메시지** 를 선택합니다.
- **발신자** 항목에서 알림을 받을 번호를 지정합니다.
  - 특정 번호만 받으려면 연락처에서 선택합니다.
  - 모든 번호에서 받으려면 비워둡니다.
- **메시지 수신 시** 가 선택되어 있는지 확인합니다.
- **다음** 을 탭합니다.

**4. 실행 방식 설정**
- **바로 실행** 토글을 켭니다. (메시지 수신 즉시 자동 실행)
- **실행 시 알리기** 토글을 끕니다. (알림 없이 조용히 실행)
- **다음** 을 탭합니다.

**5. 동작 추가**

아래 순서대로 동작을 추가합니다.

**a. 메시지 내용 가져오기**
- **동작 추가** 탭 → **스크립트 작성** → **단축어 입력** 을 추가합니다.
  - 이 동작은 자동화를 트리거한 메시지의 내용과 발신자를 담고 있습니다.

**b. URL 설정**
- **동작 추가** 탭 → 검색창에 **URL** 입력 → **URL** 동작을 추가합니다.
- URL 입력란에 서버 주소를 입력합니다:
  ```
  https://your-server.onrender.com/webhook
  ```

**c. URL 내용 가져오기 (HTTP 요청)**
- **동작 추가** 탭 → 검색창에 **URL 내용 가져오기** 입력 → 동작을 추가합니다.
- 설정 값:
  - **URL**: (이전 단계의 URL 변수가 자동 연결됩니다)
  - **방법**: `POST`
  - **헤더**: `+` 탭하여 추가
    - 키: `Content-Type`
    - 값: `application/json`
  - **본문**: `JSON` 선택 후 아래 3개 항목을 `+` 로 추가:

    | 키 | 값 |
    |---|---|
    | `sender` | **단축어 입력** 의 **발신자** |
    | `message` | **단축어 입력** 의 **내용** |
    | `token` | 서버 AUTH_TOKEN 값을 직접 입력 |

    - `sender` 항목 값 입력 시: 텍스트 필드를 탭하면 **단축어 입력** 변수가 보입니다. 선택 후 **발신자** 를 탭합니다.
    - `message` 항목 값 입력 시: 마찬가지로 **단축어 입력** 을 선택 후 **내용** 을 탭합니다.
    - `token` 항목 값: 서버에 설정한 AUTH_TOKEN 값을 직접 타이핑합니다.

**6. 완료**
- **완료** 를 탭하여 저장합니다.

---

## 5단계: 테스트

1. **테스트 문자 보내기**
   - 본인 또는 지인에게 부탁하여 인증번호 형식의 문자를 보냅니다.
   - 예시 문자: `[테스트] 인증번호는 123456입니다.`

2. **서버 로그 확인**
   - Render 대시보드 → 서비스 선택 → **Logs** 탭에서 실시간 로그를 확인합니다.
   - 정상 처리 시 아래와 같은 로그가 표시됩니다:
     ```
     웹훅 수신 | 발신: 01012345678 | 메시지: [테스트] 인증번호는 123456입니다.
     인증번호 감지 | 발신: 01012345678 | 코드: 123456
     Telegram 전송 성공
     전달 결과 | {'telegram': '성공', 'wecom': '비활성화'}
     ```

3. **Telegram/WeCom 수신 확인**
   - 설정한 채팅에서 아래 형태의 메시지가 수신되는지 확인합니다:
     ```
     🔐 인증번호 감지
     발신: 01012345678
     코드: 123456
     원문: [테스트] 인증번호는 123456입니다.
     시각: 2026-04-08 14:30:25
     ```

4. **헬스체크 엔드포인트**
   - 브라우저에서 `https://your-server.onrender.com/health` 를 열어 서버 상태를 확인합니다.

---

## 주의사항

- **AUTH_TOKEN 보안**: 반드시 길고 랜덤한 값으로 설정하세요.
  `openssl rand -hex 32` 명령으로 생성할 수 있습니다.
- **Render 무료 플랜 슬립**: 15분 비활동 시 슬립 상태가 됩니다.
  첫 요청 시 최대 30초 지연이 발생하지만 인증번호 유효시간 내에 충분합니다.
- **iOS 버전**: iPhone 단축어 자동화의 "바로 실행" 기능은 iOS 15.4 이상에서 지원됩니다.
- **개인정보**: 인증번호는 민감한 정보입니다. 서버 로그를 외부에 노출하지 않도록 주의하세요.
